# Copy to .env.local and fill in. .env.local is gitignored.

# Prodia API token — create one at https://app.prodia.com (API Dashboard).
# Read only on the server by src/app/api/generate/route.ts.
PRODIA_TOKEN=
